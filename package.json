{
  "name": "what-a-drag",
  "private": true,
  "version": "0.0.1",
  "description": "what-a-drag",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "surge": "cd www && surge --domain limping-play.surge.sh",
    "deploy": "npm run build && npm run surge",
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch"
  },
  "dependencies": {
    "@ionic/core": "one"
  },
  "devDependencies": {
    "@stencil/core": "1.0.0-beta.8"
  }
}
