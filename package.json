{
  "name": "what-a-drag",
  "private": true,
  "version": "0.0.1",
  "description": "what-a-drag",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch"
  },
  "dependencies": {
    "@ionic/core": "^4.11.4",
    "@stencil/state-tunnel": "^1.0.1"
  },
  "devDependencies": {
    "@stencil/core": "^1.7.5",
    "@stencil/postcss": "^1.0.1",
    "postcss-preset-env": "^6.6.0"
  }
}
